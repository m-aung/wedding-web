---
"wedding-web": patch
---

Fix image src paths — use root-relative paths (/filename) instead of relative public/ prefixes so images load correctly in production. Also rename wedding_parking_map.jpeg to wedding-parking-map.jpeg for consistency.
